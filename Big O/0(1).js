function funChallenge(input){
    let a = 10; // O(1)
    a = 50 + 3; // O(1)

    for (let i = 0; i < input.length; i++) {
        anotherFunction(); // Assuming O(1)
        let stranger = true; // O(1)
        a++; // O(1)
    }
    
    return a; // O(1)
}
